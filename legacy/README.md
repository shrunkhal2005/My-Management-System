# Legacy Files

This folder contains the old monolith helper scripts, notes, and archive files that were moved out of the repository root to keep the microservices layout easier to scan.

Nothing here is part of the current `auth-service` / `user-service` runtime path.